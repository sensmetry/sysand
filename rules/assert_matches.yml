# Fix does not import std::assert_matches
id: assert-matches
severity: warning
language: rust
rule:
  pattern: assert!(matches!($$$ARGS))
fix: |
  assert_matches!($$$ARGS)
message: "manual `assert_matches!()`"
